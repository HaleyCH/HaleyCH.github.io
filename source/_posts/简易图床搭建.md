title: FastAPI+MongoDB搭建简易本地图床，可将图片同步到各大图床
author: HaleyCH
tags:
  - fastAPI
  - MongoDB
  - python
  - pymongo
cover_image: 'http://i.hluvmiku.tech/f/63b6ae7192b1c6a2b00373c2/'
categories:
  - 博客搭建
abbrlink: 5be9159
date: 2023-01-05 18:25:00
---

![img_63b6ae7192b1c6a2b00373c2](http://i.hluvmiku.tech/f/63b6ae7192b1c6a2b00373c2/)
# 前言

博客，不能没有图片。
但把所有图片都丢自己服务器，也很麻烦。
全放到像七牛云啊啥的又感觉不安全啥的。

于是我便花了一个晚上写了个简易的图床
让它能在本地存图片，同时以lazy的方式把图片上传到各大图床。
用户访问时，优先重定向访问各大图床上的图片。

原理图：

![img_63b6a9d692b1c6a2b00373c0](http://i.hluvmiku.tech/f/63b6a9d692b1c6a2b00373c0/)

已经部署到 [i.hluvmiku.tech](http://i.hluvmiku.tech)上了
# API接口

利用 `fastAPI` 搭建，需实现以下几个API接口：

|路由|method|功能|
|----|------|----|
|`/`|Get|上传界面|
|`/`|Post|上传图片至mongoDB，返回图片对应链接`/f/<pk>`|
|`/raw/<pk>/`|Get|返回pk对应本地图片|
|`/f/<pk>/`|get|图片链接。重定向，若已经上传至图床，重定向到图床中图片对应链接，否则重定向到`/raw/<pk>/`|

浏览器等直接使用的链接为 `/f/<pk>/`。

# 后端

后端使用 `pymongo` 实现。
过程不赘述。各部分流程如下：

- `/` 获得Post的图片，验证是否安全，若不安全，返回。若安全，转换为base64，计算sha256作为`<pk>`，储存到数据库。
- `/f/<pk>/` 收到Get请求，检查本地有无各大图床对应链接。若有，返回各大图床中的链接；若无，重定向`/raw/<pk>`链接，并将图片加入上传队列。
- `/raw/<pk>/` 收到Get请求，返回本地数据库中图片。

# 前端

随便写写。

# 总结

随便写写的，但是感觉蛮有意思，等我重构完会开源，现在代码不是很美观。